# v0.7.2 local acceptance — 2026-09-15

## Automated checks

- Backend: 824/824 tests passed; desktop: 390/390; shared: 13/13. Total: 1,227.
- TypeScript checks, lint command and upgrade fixtures exited 0.
- Lint retains 27 warnings and one informational diagnostic; there are no formatter/check errors.
- Regression coverage includes late acknowledgements, rejected queued sends, repeated UI-only commands, custom Ask choices, SDK abort/error results and ignored cancellation.
- Knowledge validation drains a bounded pending watcher batch; changed revisions, unread/changed sources and incomplete coverage still fail validation.

## Actual packaged application

macOS arm64, application 0.7.2, Electron 43.3.0, embedded Node 24.18.1.
The DMG was verified, mounted read-only, copied to an isolated installation directory and launched there.
Deep signature verification passed for both the build and the installed copy. Signing is ad-hoc, not notarized.
Packaged-resource checks passed: 36 core tools, four bundled skills, SQLite worker and read-only child interface; no ambient skills/private config.

Seven application checks passed through Chromium input, native IPC and the real SDK:
1. Three Wiki review open/close cycles, zero provider requests and no fabricated agent starts.
2. Immediate answer completion without a late-ack running-state regression.
3. Single-question custom input, sequential answers and confirmed setup writes: zero Markdown files before approval, 16 afterward.
4. Stop remains available with a draft and closes the streaming connection.
5. An expanded error card preserves the original upstream 503.
6. Native provider probe succeeds against a loopback fixture.
7. A hanging probe returns after 15.077 seconds, with one request and the underlying connection closed.

The seven deterministic scenarios use a scripted loopback provider, not a research-capability benchmark.
A separate native probe using the existing DeepSeek configuration succeeded (`deepseek-v4-flash`, 973 ms); credentials were neither copied nor printed.
That real-provider check was only a minimal ping, not a complete live-model research workflow.

External MCP authentication, long real-model research jobs and other operating systems' real-machine behavior are not certified by this local acceptance.
Other-platform release builds are handled separately by GitHub Actions.
