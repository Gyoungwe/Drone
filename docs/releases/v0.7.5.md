# Drone v0.7.5

## Fixed

- **About page update action.** When a newer version is found, the update button now starts the download. After the download completes, the existing restart-and-install action finishes the in-place update without requiring an uninstall.

## Validation

- Desktop typecheck passes.
- Desktop update-policy tests pass (4 tests).
- Release artifacts are built by the GitHub Actions release workflow for macOS, Windows, and Linux.
