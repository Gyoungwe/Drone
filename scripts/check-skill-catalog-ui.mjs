// Compatibility entry point: the catalog now defaults to six workflow directions.
// Keep existing validation commands working against the current, isolated Electron fixture.
import "./check-workflow-catalog-ui.mjs";
